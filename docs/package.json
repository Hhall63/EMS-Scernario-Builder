{
  "scripts": {
    "pretest": "node -v && npm -v",
    "test": "npm run test:unit",
    "test:unit": "jest --runInBand --coverage --config=jest.config.js",
    "test:e2e": "playwright test --reporter=list,junit",
    "validate:json": "node scripts/validate-scenarios.js",
    "lint": "eslint \"**/*.{js,jsx,ts,tsx}\" --max-warnings=0",
    "test:all": "npm run validate:json && npm run lint && npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@eslint/js": "^9.8.0",
    "@playwright/test": "1.46",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "eslint": "^9.8.0",
    "globals": "^15.8.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  }
}
